todo :

- sur le ticket detail date de l'evenement et du début

- sur la page achat bouton valider css et scroll$

- sur le qr code preview mettre le valider au milieu

- implémenter le support ( envoi email etc)

- à l'achat d'un ticket, le rendre nominatif

- code et profil organisateur

optionnel :

- pas oublier les modal
- documentation
- tests unitaires ??


hooks/
├── useAuth.ts            // Gestion auth
├── useForm.ts            // Validation formulaires
├── useVerifyIdentity.ts  // Vérification identité
└── useTickets.ts         // Gestion tickets

utils/
├── validation.ts         // Validation formulaires
├── formatting.ts         // Formatage (dates, téléphone, etc.)
├── crypto.ts             // Encryption/hachage
└── helpers.ts            // Autres utilitaires